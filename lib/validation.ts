// Zod schemas shared between client-side forms and server-side validation
// (including Edge Functions). Implemented alongside the features that need
// them, starting with the booking flow in Block B5.
export {};
