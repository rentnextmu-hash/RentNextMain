// Placeholder until the real schema exists (see supabase/migrations).
// Regenerate with `npm run types:gen` once the migrations in P1.1 are
// applied to the linked Supabase project — this file is then fully
// overwritten by the Supabase CLI.
export type Database = {
  public: {
    Tables: Record<string, never>;
    Views: Record<string, never>;
    Functions: Record<string, never>;
    Enums: Record<string, never>;
  };
};
